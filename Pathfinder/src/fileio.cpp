#include "fileio.hpp"

namespace pathfinder {
    namespace fileio {
        Reader::Reader(objects::Graph* graph) {
            this->graph = graph;
        }
        
        void Reader::Fill() {
            
        }
        
        objects::Graph* Reader::ReadEdge() {
            
        }
        
        objects::Node* Reader::ReadNode() {
            
        }
    }
}